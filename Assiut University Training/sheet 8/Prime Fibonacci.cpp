#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;

    vector<int> test_cases(t);

    for (int i = 0; i < t; i++)
    {
        cin >> test_cases[i];
    }

    for (int i = 0; i < t; i++)
    {
    }
}